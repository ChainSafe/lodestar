#!/usr/bin/env bash

# Convenience script to run the lodestar binary from built source
#
# ./lodestar.sh beacon --network prater

UV_THREADPOOL_SIZE=64 node --trace-deprecation --max-old-space-size=4096 ./packages/cli/bin/lodestar.js "$@"