/*
import { altair } from "@chainsafe/lodestar-types"
import { CachedBeaconState } from "../../../fast";

export function processBlock(state: CachedBeaconState<altair.BeaconState>, block: altair.BeaconBlock): void {
  processBlockHeader(state, block);
  processRandao(state, block.body);
  processEth1Data(state, block.body);
  processOperations(state, block.body); // [Modified in Altair]
  processSyncCommittee(state, block.body.syncAggregate); // [New in Altair]
}
*/
